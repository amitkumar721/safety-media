<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Log;
class SpireController extends Controller
{
    // public getSpireAddressData($customerNumber = null) {
    //     $authorizationSpire = config('config.Spire_Api_Auth');
    //     $url =  config('config.Spire_Customer_Api_Url') . '?filter={"customerNo":"' . $customerNumber. '"}';
    //     $spireApiResponse = call_curl($url, $method = "GET",  $payload = '', $authorizationSpire);
    //     Log::info($spireApiResponse);
    // }
}
